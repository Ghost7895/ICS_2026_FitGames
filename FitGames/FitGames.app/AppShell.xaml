<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="FitGames.app.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:FitGames.app"
    xmlns:libraryViews="clr-namespace:FitGames.app.Views.Library"
    xmlns:signInViews="clr-namespace:FitGames.app.Views.SignIn"
    x:Name="AppShellRoot"
    FlyoutBehavior="Disabled"
    Title="FitGames.app">

    <Shell.MenuBarItems>
        <MenuBarItem Text="File">
            <MenuFlyoutItem Text="Home"
                            Command="{Binding Source={x:Reference AppShellRoot}, Path=GoToHomeCommand}" />
            <MenuFlyoutItem Text="Library"
                            Command="{Binding Source={x:Reference AppShellRoot}, Path=GoToLibraryCommand}" />
        </MenuBarItem>
    </Shell.MenuBarItems>

    <TabBar>
        <Tab Route="signin">
            <ShellContent ContentTemplate="{DataTemplate signInViews:SignInPage}"
                          Shell.TabBarIsVisible="False" />
        </Tab>
        <Tab Route="home">
            <ShellContent ContentTemplate="{DataTemplate local:MainPage}" />
        </Tab>
        <Tab Route="library">
            <ShellContent ContentTemplate="{DataTemplate libraryViews:LibraryPage}" />
        </Tab>
    </TabBar>

</Shell>
